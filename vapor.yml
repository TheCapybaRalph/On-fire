id: 75200
name: onfiremonitoring
environments:
  production:
    memory: 1024
    cli-memory: 512
    database: pg-prod-db
    runtime: docker
    docker-build-options:
      - provenance=false
    build:
      - 'herd composer install --no-dev'
      - 'herd php artisan event:cache'
      - 'npm i && npm run build'
    deploy:
      - 'php artisan migrate --force'
