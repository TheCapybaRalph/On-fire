id: 75225
name: jett-revive-me
environments:
  production:
    memory: 1024
    cli-memory: 512
    database: pgqq
    cache: cachi
    runtime: docker
    docker-build-options:
      - provenance=false
    build:
      - 'composer install --no-dev'
      - 'php artisan event:cache'
    deploy:
      - 'php artisan migrate --force'
